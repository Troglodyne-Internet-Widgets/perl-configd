*.swp
*.swo
*.swn

# Build leavings
MYMETA.json
MYMETA.yml
Makefile
pm_to_blib
blib/
cover_db/
.build

# The tarball dzil builds and the directory it builds it from, anchored to the
# root: unanchored, `Configd-*` also matches the tests named after the modules
# they cover, and `git add` skips an ignored file without saying so.
/Configd-*.tar.gz
/Configd-*/
